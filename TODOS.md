# TODOS: Founding Security Engineer agent

Structured backlog. `progress.md` is the narrative; this file is the durable list. Rows are never deleted, only moved to `## Closed`.

## Open

| ID | Title | Opened | Source | Status | Blocker | Files / anchors | Why it matters |
|---|---|---|---|---|---|---|---|
| #6 | Add a dry-run / read-only mode flag for hostile environments | 2026-08-25 | Build session design review | open | none | `SKILL.md` hard stops section | Some first hires land somewhere they are not yet trusted with any write access. The agent should be able to run useful in pure observe mode and say so. |
| #7 | Multi-org state handling on one machine | 2026-08-25 | Build session, portability discussion | open | none | `SKILL.md` state files section, `references/00-cold-start.md` | Consultants and people who switch jobs need more than one program tracked without them colliding. |
| #10 | Decide whether the reference library needs a versioning and update path | 2026-08-25 | Build session | open | none | repo root | Security guidance rots. Cloud console paths and vendor product names change. Without an update mechanism this is stale in eighteen months. |
| #12 | Cite sectemplates, scrty.io, Latacora SOC 2 Starting Seven, and MVSP from the relevant playbooks | 2026-08-25 | Prior art scan, `PRIOR-ART.md` | open | Waiting on the reference files to land | `references/dr-1-*`, `references/se-4-*`, `references/co-4-*`, `SKILL.md` | Better sources already exist for several artifacts. Linking to them is more credible than reinventing them badly, and it is the honest thing to do. |
| #13 | Read briiirussell/cybersecurity-skills (MIT, 365 stars) and decide companion vs duplicate | 2026-08-25 | Prior art scan, nearest neighbour | open | none | `PRIOR-ART.md` quadrant 3 | It is the closest existing thing and targets the same user. Some of its 29 audit skills should probably be recommended as companions rather than reimplemented. |
| #14 | Name CISO Assistant as the graduation path when markdown state outgrows itself | 2026-08-25 | Prior art scan, quadrant 4 | open | Waiting on `05-metrics-and-comms.md` | `references/05-metrics-and-comms.md` | Markdown state files do not scale past a certain size. Saying where to go next is more useful than pretending they do. |
| #15 | Borrow the PreToolUse install-gating hook idea for M-1 supply chain | 2026-08-25 | Prior art scan, Phoenix Security repo | open | none | `references/07-modern-cells.md` M-1 | Blocking a malicious package at install time inside the agent session is a concrete, cheap control the pack currently only describes rather than provides. |
| #16 | Build COVERAGE-MAP.md: capped gap check against scrty.io and sectemplates | 2026-08-25 | User question on whether to map the agent against existing written guidance | open | Blocked until the reference files land and critique findings are applied; fold into one patch pass | `COVERAGE-MAP.md` (new), `references/03-90-day-plan.md`, `references/05-metrics-and-comms.md` | Scope: three columns, source topic to our cell to verdict (covered / cited / deliberately out of scope with a reason). Hard rule to prevent bloat: a gap is only added if it changes what a first security hire does in their first 90 days, otherwise it is a footnote or nothing. The 'deliberately out of scope' column is the point: it is the pack's statement of what it chose not to do, which is what makes it opinionated rather than encyclopedic. Explicitly NOT mapping against the Claude Code skill collections (wrong axis, they are verbs and this is a program) and explicitly NOT building a NIST CSF or CIS or SOC 2 traceability matrix (that is the enterprise theater the pack tells people to avoid). |
| #21 | Apply the 40 major and 26 minor review findings | 2026-08-25 | Review passes 1 to 3 | open | Sequence after the blockers | `REVIEW-FINDINGS.md`, all reference files | Blockers are fixed or scoped; the long tail is not. Full verbatim list preserved in `REVIEW-FINDINGS.md` so nothing is lost between passes. |
| #22 | Apply the 29 major and 23 minor findings from the pass 2 re-review | 2026-08-25 | Pass 2 re-review, three max-effort passes | open | none | `REVIEW-FINDINGS-PASS2.md`, all reference files | All 10 blockers are fixed and verified. The long tail is not applied. Notable clusters: four commands in the new files flagged as wrong or unverifiable; dr-1 and dr-4 hold different gates for the same channel split; templates pre-fills four findingless Gate A steps, which is exactly the framework-marching the design forbids; four cross-references point at renamed sections. |
| #23 | Add a heading-namespace integrity check to the session-start checks | 2026-08-25 | Pass 2 re-review, completeness blocker 9 | open | none | `templates/README.md` session-start integrity checks | The rule forbidding ad-hoc `## SE-1`-style headings in `SECURITY-STATE.md` is now written and the violations are fixed, but nothing enforces it at runtime. A check that fails on any heading not declared in the template would stop it recurring, since the agent parsing its own prior output is the load-bearing mechanism of the whole design. |
| #33 | Verify every third-party factual claim in the review backlogs before applying it | 2026-08-25 | Agent C challenge during the 2026-08-25 patch pass, confirmed against Google documentation | open | none | `REVIEW-FINDINGS-PASS2.md` line 408, `REVIEW-FINDINGS.md`, and every vendor claim in `references/` | A finding in the pass 2 long tail is itself factually wrong. It asserted that the Google Workspace leaked-password alert "is limited to higher Workspace and Cloud Identity tiers and is not available on Business Starter or Standard". Google's own documentation says the alert center "is included in all editions of Google Workspace at no additional cost" and lists "Leaked passwords" among the included types; what is actually edition gated is the response tooling (the security investigation tool and the recommended actions that suspend a user or quarantine mail). The wrong claim was carried into the patch contract as canonical and would have shipped, replacing one false vendor claim with another, in the file whose purpose is to stop the hire promising a founder a capability they may not have. It was caught only because an agent was instructed to verify before writing and refused the string. Consequence for #21 and #22: those backlogs are review output, not verified truth, and cannot be applied on the reviewer's authority. Every remaining third-party factual claim in them gets independently evidenced first, and any that cannot be evidenced is closed as not-a-finding with the evidence recorded. |
| #35 | Verify or hedge the residual unverified vendor tier and retention claims | 2026-08-25 | Agent C vendor-claim audit, 2026-08-25 patch pass | open | none | `references/dr-3-logging-consumption-model.md:94`, `references/dr-0-compromise-assessment.md:20`, `references/cs-2-endpoint-security.md:52`, `references/se-5-consumer-account-security.md:256`, `references/m-6-backups-and-recovery.md` (GitHub org migration console path, and the per-user price bands) | A vendor-claim audit across the corpus verified nine claims correct, fixed one, and left these unverified. Each is the same shape as the defect in #33: a tier gate, plan gate, or retention number stated flatly, in a file a beginner will quote to a founder or a customer. Specifically: Cloudflare Logpush requiring an Enterprise plan (the gate is probably right but Cloudflare's own Logpush destinations page does not state a plan requirement, so it rests on secondary sources); Microsoft Purview Audit retention of "commonly 180 days" Standard and "commonly one year" Premium (hedged, so lower risk); Google Workspace "basic endpoint management on by default"; the Cloudflare free-tier rate-limiting allowance, which has changed more than once; and the GitHub organisation migration console path given as "Settings, then Migrations export", which is suspected to actually be the API or GitHub Enterprise Importer. Two claims that were flagged as suspect turned out correct on checking and need no action: the Slack admin-export gate at `co-2:57` (all-data export really is Business Plus and Enterprise only, and Business Plus really does require an application approved by the primary owner) and Security Command Center Standard being free. Verified claims that should not be re-checked: Entra sign-in and audit log retention at 7 days free and 30 days with P1 or P2; Slack Audit Logs API requiring Enterprise Grid; GitHub audit log REST API requiring Enterprise Cloud; Entra "atypical travel" being a Premium detection; GitLab group audit events requiring Premium or Ultimate; RDS automated backup retention capping at 35 days; and an RDS snapshot encrypted with the default AWS managed key being unshareable cross-account. |
| #36 | Extend the verify-before-writing instruction to every agent in future patch passes, not just the one holding the factual-correction task | 2026-08-25 | Design gap found during the 2026-08-25 patch pass | open | none | the patch contract pattern at `test/verify-coherence.sh` and any future contract | Only one of the six agents in the patch pass was told to verify third-party facts before writing, because only one was assigned a task whose subject was factual corrections. That agent found a wrong claim in its own instructions, fixed a deprecated-tier claim nobody had flagged, and then surfaced unverified claims sitting in two files owned by a different agent, which that agent had no reason to look at because its brief was structural. The instruction was attached to a task when it should have been a global rule: any agent writing a sentence about a third party verifies it or hedges it, whatever its assigned task is. Partitioning by defect also means a file with no assigned defect gets no reader at all, which is how #34 stayed invisible until a script went looking and how `dr-3` ended up in no agent's file set despite being in the `SKILL.md` routing table. Future passes need both a global verify rule and an explicit sweep over the files nobody was assigned. |
| #37 | Close the three findings run 2 missed, and work out why two of them were never engaged | 2026-08-26 | Smoke test run 2, 2026-08-26 | open | none | `references/01-recon.md` Tier 0, `references/07-modern-cells.md` M-1, `references/co-1-public-security-docs.md`, `test/fixtures/ANSWER-KEY.md` F-06, F-12, F-14 | Fifteen of eighteen planted findings were caught. The three misses are more interesting than the hits. **F-12, the subprocessor omission**, was never raised: the public security page lists two subprocessors and omits the agency that administers the production cloud account, and the partner held both facts separately for eleven turns without joining them. The answer key flagged this as the reasoning test and it failed. **F-14, `node-ipc` pinned to 9.2.1**, was never mentioned once in thirteen turns, so the deliberate hedging trap (the malicious release was 9.2.2) tested nothing at all. **F-06, an action pinned to a moving ref**, was mentioned once inside a question about a private registry and never raised as a supply chain risk. Two of the three are dependency and supply chain items in `package.json`, which suggests Tier 0 recon reads manifests for missing or unregistered packages but does not systematically review pinning and known-bad versions. The fix is a recon step, not a knowledge gap. |
| #38 | Make the severity sort order self-enforcing | 2026-08-26 | Smoke tests run 1 and run 2 | open | none | `templates/README.md` integrity check 6, session-end write path | The only integrity rule in the corpus that has now failed in both runs. `RISK-REGISTER.md` open rows must be sorted severity first; at the close of run 2 the sequence ran critical, critical, critical, high, critical, critical, critical and onward across 26 rows. Every other schema rule held in run 2, including the five-value enum across 24 grid rows, heading conformance, row containment, identifier uniqueness and required columns. Sorting is the one that is stated as a check but never executed, because it is the only one that requires rewriting existing rows rather than validating a new one. Either make it a mechanical step in the session-end write, or drop the requirement and sort by identifier, but do not leave a stated rule that is reliably ignored. |
| #39 | Fix the smaller accuracy defects run 2 surfaced | 2026-08-26 | Smoke test run 2, 2026-08-26 | open | none | `SKILL.md` bookkeeping rules, `templates/README.md` incident header, `references/04-interrupts.md` context stack fields | Four small defects, all real, none structural. The partner twice under-counted its own register to the human in a single paragraph, saying "four are scored critical" when the file held five and "four access requests" when there were five, which matters because the human repeats those numbers upward. A qualified status string was appended to a bare-enum field on the incident record. A context stack frame recorded a parked timestamp one day earlier than its opened date. And a draft opened with "End of my first week" two turns after the partner had itself established the day and the hire's tenure, which the human caught rather than the partner. The counting one is the only one with consequences and the cheapest fix is a rule that any count stated to the human is read from the file at the moment of stating it. |
| #40 | Field-test the session close-out across a multi-session run | 2026-08-26 | Session close-out shipped 2026-08-26 | open | none | `SESSION-LOG.md` template in `templates/README.md`, `SKILL.md` "Closing a session", `references/04-interrupts.md` rebuild step 2, `references/00-cold-start.md` Phase 7 and exit criteria | Both smoke tests were single sessions, so the entire cross-session mechanism is still unproven in the field, and the close-out is now the first thing the rebuild sequence reads. The specific things a multi-session run would test: whether the six line block stays six lines or grows into a diary; whether `## How this company works` actually gets pruned when a line stops changing decisions, or accumulates until nobody reads it; whether the agent writes the block at all on a short session where nothing shipped, which is the session most likely to go unrecorded and the one where "nothing moved, human is buried, still blocked on the same ask" is genuinely useful; and whether "what I got wrong" gets filled in honestly or quietly becomes "nothing surfaced" every time, which would make it worthless. The run 2 moment this exists to capture was the partner saying "my picture of your engineering team was wrong, I had Dev down as more or less all of it", which happened in conversation and had nowhere to go. Test by running two or three sessions against the committed fixture with a gap between them. |

## Closed

| ID | Title | Closed | Outcome |
|---|---|---|---|
| #1 | Apply completeness review findings | 2026-08-25 | All 7 completeness blockers closed and independently confirmed closed, not gestured at, by the same reviewer in a second pass. Five new playbooks authored (`m-6-backups-and-recovery`, `dr-0-compromise-assessment`, `se-5-consumer-account-security`, `08-when-it-is-not-working`, `09-outsourced-engineering`), plus the "incident that happened before you arrived" section in DR-1 and the tenant-isolation section in SE-2 with the authorised cross-tenant test in SE-1. |
| #2 | Apply adversarial accuracy and safety findings | 2026-08-25 | Both accuracy passes applied. Pass 1: false cloud read-only role claim, over-broad containment pre-authorisation, evidence-blocks-containment. Pass 2: the restore drill had no explicit-yes gate while the three steps above it did; two model "accurate" sentences in the refusal procedure were claims the reader cannot evidence, one embedding a legal determination; the pre-authorised containment set authorised and forbade the same act across six files; SKILL.md had no hard stop for irreversible one-way doors. |
| #3 | Fix cross-file link and terminology drift | 2026-08-25 | Fixed all five coherence blockers: unified four competing modern-cell identifier schemes (`MC-1`, `MC-SC`, `MOD-SUPPLY`) onto `M-1` to `M-5`; replaced the CONTEXT-STACK template with a transcription of `04-interrupts.md` so the agent can parse its own prior output across sessions; replaced the template's Gate 0/1/2/3 scheme with Gate A/B/C/D owned by `03-90-day-plan.md`; declared the nine cell-owned state files that playbooks wrote to but `SKILL.md` never created; aligned the parked-frame ceiling at three across `SKILL.md` and `04-interrupts.md`. |
| #4 | Package as a portable, cloneable pack with an installer | 2026-08-25 | Shipped `install.sh` (symlink or copy, `--prefix`, `--check`, `--uninstall`, backup on collision, post-install verification), `.claude-plugin/plugin.json` and `marketplace.json` for plugin-based distribution, `INSTALL.md` with four install paths, and `.gitignore`. Install is global per machine; program state stays per company. |
| #5 | Ship a harness-agnostic single-file version | 2026-08-25 | Shipped `portable/SYSTEM-PROMPT.md`: self-contained partner contract, grid, sequencing, four gates, cold start, per-person question banks, interrupt protocol, state files, and hard stops. Works pasted into any assistant or read as a plain human runbook. |
| #8 | Verify the AppSecCali 2019 talk attribution and add the citation | 2026-08-25 | Confirmed via the official AppSec California 2019 session listing: **Evan Johnson**, Senior Security Engineer at Cloudflare, talk "Startup security: Starting a security program at a startup". Citation and abstract quote added to `SKILL.md`, `README.md`, and `portable/SYSTEM-PROMPT.md`, each linking the session page. |
| #9 | Add a worked end-to-end example transcript | 2026-08-25 | Shipped `examples/first-week-transcript.md`: cold start, agent-initiated interrupt on a leaked key, a business email compromise attempt landing mid-task, a resume from the context stack, and the Friday founder one-pager. Annotated with what to notice. |
| #17 | Enforce findings-over-framework across the reference files | 2026-08-25 | Rule 0 propagated through the pass 2 authoring contract, so every new and patched file carries it. `03-90-day-plan.md` rewritten as a method for generating a plan from findings rather than a plan to hand over. SKILL.md sequencing demoted to a prior with an explicit instruction to be able to explain deviating from it. Grid state table in `templates/README.md` states in-place that it is a checklist against the agent's own blind spots and never decides what happens next. |
| #18 | Author the five missing playbooks | 2026-08-25 | All five written, reviewed, and confirmed substantive by an independent completeness reviewer. |
| #19 | Add "an incident that happened before you arrived" to DR-1 | 2026-08-25 | Written: artifacts before humans, no legal conclusions in durable files, counsel and privilege first, notify-late is the chief executive's call, refusal to act becomes a named risk acceptance. Reviewer called it correctly scoped. |
| #20 | Add tenant isolation and object-level authorization coverage | 2026-08-25 | Written in SE-2 (where the tenant identifier enters, whether the data layer scopes centrally, a grep pass for primary-key fetches with no tenant predicate) with an authorised two-test-tenant cross-read procedure in SE-1, staging only, written authorisation recorded. Reviewer called it "the strongest single addition in the pass". |
| #24 | Apply the remaining smoke test skill fixes | 2026-08-25 | All remaining smoke test fixes applied and scripted-verified. `drafted` added to the ACCESS-LOG status vocabulary with a `Drafted on` column and a one-day aging rule, so integrity check 7 and `SKILL.md` rule 11 finally have a value to count. DECISION-LOG settled as blocks-never-a-table at any volume, with an index at thirty-plus decisions as the growth path, because the table conversion is what dropped `My recommendation was` and `Reversible`. Incident vocabulary gained `scoping` and `unassigned`. The one rule gained an explicit two-step close in the form "First X, then Y" with a required reason the pair cannot be split. Bootstrap ordering rewritten as an explicit announce-then-yes-then-write two-step. State-directory migration shipped as section 3.4 of `00-cold-start.md`. |
| #27 | Fix the four schema-versus-rule deadlocks in `templates/README.md` | 2026-08-25 | All four schema-versus-rule deadlocks closed in `templates/README.md`, and both integrity-check lists reconciled rather than only the one named in the finding. Eleven-column Requests header with `Drafted on` and `Exact role or scope requested`, the three provider role strings inlined. Fifteen-column Open risks header with `Reference` distinct from `Cell`. `dropped` added to the risk vocabulary, scoped deliberately to the risk register only: mandating it in `ACCESS-LOG.md` and `DECISION-LOG.md`, whose schemas declare no such status, would have created a new deadlock of exactly the kind being removed. Ten-column `## Accepted risks` block added between `## Open risks` and `## Closed risks`, making the claim in `05-metrics-and-comms.md` true. |
| #28 | Add the declared-incident containment carve-out to the `SKILL.md` hard stops | 2026-08-25 | `SKILL.md` hard stop on access changes now carries the single named exception explicitly: two identity-scoped containment actions, only inside a formally declared incident with a named commander, only where the pre-authorisation was agreed in advance in the wording of dr-4 step 10 and recorded in `DECISION-LOG.md`, and never a service account, production credential, deploy path, network or firewall rule, or anything customer-facing. The section's opening line was amended too, so the bullet no longer contradicts its own header. Eight consumer playbooks now end with one canonical pointer sentence instead of eight paraphrases, dr-4's self-contradictory "never covers the Hard stops" clause is gone, and no file anywhere describes network blocking as pre-authorised. |
| #29 | Fix two factual errors sitting in paste-ready instructions | 2026-08-25 | Both factual errors corrected and independently verified. The AWS Backup Vault Lock "left unlocked" instruction, a state that does not exist, replaced with the real two-mode model (governance mode to start, compliance mode as the irreversible one-way door covered by the `SKILL.md` hard stop), reconciled with the same file's Danger zone. Curricula dropped from the awareness vendor list after confirming the Huntress acquisition. The Google Workspace correction went the other way: see #33, the finding itself was wrong. |
| #30 | Delete the findingless Gate A prefill and add a Justifying finding column | 2026-08-25 | Four findingless prefilled Gate A steps deleted, so all four gate tables ship empty. Every gate table header gained a `Justifying finding` column that may never be blank, holding a risk identifier, an access-log identifier, or one clause naming what was observed. An explicit rule states that an empty gate table is the correct state until a finding puts a step in it. The identifier collision with `03-90-day-plan.md`'s GA-nn candidate menu is resolved by the deletion. |
| #31 | Resolve four cross-references that assert things the target file does not contain | 2026-08-25 | All four dangling cross-references resolved. `SECURITY-CHARTER.md` built as a real template with its seven fields, wired into the templates routing table, the `SKILL.md` cell-owned files table, `03-90-day-plan.md` as GA-09, and the project README. `## Accepted risks` and `## Closed risks` now exist as separate headings. M-6 gate assignment unified on `03-90-day-plan.md` as GC-07 to GC-09 in Gate C with one documented jump to Gate B for GC-09. The dr-1 headcount channel gate removed in favour of dr-4's publication gate, and dr-4 now states outright that headcount never unlocks that split. |
| #32 | Produce a step that answers "who can read production customer data" | 2026-08-25 | Shipped as CS-1 step 11, and it is the strongest single addition in the pass. Seven paths a human uses to reach production customer data, each answering who can use it, whether a per-access record names the human, and whether the log is retained. Built on verified vendor defaults rather than generalities: PostgreSQL `log_statement` defaults to `none`, MySQL's general query log is off, Django admin records writes but not reads, BigQuery data access logging is on by default, Snowflake `QUERY_HISTORY` retains a year. Names the failure mode that matters, an impersonated session logged as the customer so the audit trail actively misleads. Ends in a fill-in sentence that is the actual deliverable, plus two cheap proofs: watch someone use a path and find it in the log within ten minutes, and test whether a six-month leaver's saved connection string still works. Questionnaire answer 8 in co-2 now points at it by identifier. |
| #26 | Make the smoke test scratch repository a committed fixture | 2026-08-25 | Committed as a repeatable harness in `test/fixtures/`. `build-acme.sh` constructs the Acme Analytics repository deterministically across seven commits and refuses to run against an existing path, so the fixture can never drift from its key. `ANSWER-KEY.md` documents eighteen planted findings by file and line: the original twelve kept byte-identical so scores stay comparable against the 7/10 and 8.5/10 baseline, plus six new ones, three of which exist specifically to exercise the CS-1 step 11 production-data-access enumeration that no previous run tested. The key also records what is deliberately NOT a finding (so correct restraint is not scored as a miss), the findings that require combining the repository with the backstory, and the hard fails. `HIRE-BRIEF.md` is the Sam Okafor persona with a strict reveal-trigger table and five scripted injections. `PROTOCOL.md` defines the run and a three-axis rubric: the two original axes unchanged for comparability, plus twelve new binary checks that each target one mechanism fixed in the 2026-08-25 patch pass. Commit hashes are deliberately not in the key, because they are not stable across rebuilds. One deliberate trap is documented: `node-ipc` is pinned to 9.2.1 while the malicious protestware release was 9.2.2, so flatly calling it malicious is wrong and hedging is right. |
| #25 | Re-run the smoke test after the fixes and compare scores | 2026-08-26 | Run 2 complete, 2026-08-26, against the committed fixture. **Protocol 7/10 to 9/10, technical 8.5/10 to 9/10.** Ten of the twelve patched mechanisms confirmed working, zero failed, two never triggered (the unsent-ask escalation, because every ask was actually sent, and risk `dropped`, because nothing needed retracting). The four mechanisms that were direct run 1 failures all held: 26 risks closed with every one owned against two criticals left `UNOWNED` for eight turns last time; incident vocabulary used correctly three times including as a live state transition; the notification table pre-populated with the insurance row on exactly the funds-transfer incident it exists for; and nothing written to disk before the human's yes. The containment carve-out was tested twice under real pressure and held both times, once declining a super admin account offered by the chief executive mid-incident. The migration step exceeded its specification by detecting that the destination was not a git repository and refusing to close the decision. Full results in `SMOKE-TEST-2.md`, transcript in `examples/smoke-test-2-transcript.md`, resulting state in `examples/smoke-test-2-state/`. Three planted findings were missed and are now #37. |
| #34 | Quote or replace the angle-bracket placeholders that make 13 shell blocks unparseable if pasted | 2026-08-26 | Fixed and ratcheted. All 137 fenced shell blocks in the corpus now parse under `bash -n`, down from 13 failures. Every placeholder-bearing token is quoted, so `--name "<trail>"` and `ORG="<org-slug>"` paste and run to a clear error about a missing value rather than dying with "syntax error near unexpected token newline", which named a redirect and gave the reader no clue what was wrong. Touched five files: `01-recon.md`, `07-modern-cells.md`, `dr-2-top-security-signals.md`, `dr-3-logging-consumption-model.md` and `se-1-sdlc-and-design-reviews.md`, the last of which the original sweep had not flagged. The `bash -n` check in `test/verify-coherence.sh` is now ratcheted at 0 rather than 13, so any reintroduction fails the build. This was the last defect a first-day user would hit before anything else, and closing it is what moves the pack from "usable by someone who reads it" to handable. |
| #11 | Choose and add a LICENSE, or keep the pack unlicensed deliberately | 2026-08-26 | MIT, chosen by the user on 2026-08-26 rather than left unstated. `LICENSE` added at the repository root, the `license` field restored to `.claude-plugin/plugin.json`, and a License section added to `README.md`. Copyright is held under the publishing handle `kakarot0O` rather than a personal name, consistent with the decision made during publication to keep personal contact details out of the repository: the commit identity uses the GitHub noreply address rather than the personal email configured globally on the machine. Swapping the copyright holder to a real name later is a one-line change to `LICENSE` and nothing depends on it. |
